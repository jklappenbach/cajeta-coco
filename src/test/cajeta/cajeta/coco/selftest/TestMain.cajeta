package cajeta.coco.selftest;

import dev.cajeta.unit.Runner;

/**
 * Test entry: reflective discovery runs every `@Test` across the binary.
 * Returns 0 when all green, 1 on any failure — `run-tests.sh` turns that into
 * the suite's exit status.
 */
public final class TestMain {
    public static int32 run() {
        return Runner.runAll();
    }
}
