.idea
.build/
.build-ir/
.coco-lintd/
.build-t/
.cajeta/
build/
plugin/build/
plugin/.cajeta/

# The tour is a consumer project: its build/ holds coco's own artifacts
# (build/coco/{sites.tsv,link.tsv,run/,ir/,obj/,measured,...}), which are
# large, machine-specific, and regenerated by `./samples/tour/run.sh`.
samples/tour/build/
samples/tour/.cajeta/
